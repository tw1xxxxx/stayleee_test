# Only ignore things that are truly not needed for the build
video/
scripts/
parse_3/
.next/
node_modules/
*.mov
*.webm

# Do NOT ignore the public folder or images, as Next.js needs them for the frontend
# and Vercel needs them to serve static assets.
# The previous version ignored all .jpg and .png files, which broke the UI.
